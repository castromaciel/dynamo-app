import React, { useEffect } from 'react';
import {
  Alert, Text,
} from 'react-native';

const Dashboard = ({ navigation, route }) => {
  return (
    <Text>Dashboard</Text>
  );
};

export default Dashboard;
